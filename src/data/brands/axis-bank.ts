import type { ProtectedBrand } from './types.js';

export const axisBank = {
  id: 'axis-bank',
  name: 'Axis Bank',
  phrases: ['axis bank'],
  domains: ['axisbank.com'],
} satisfies ProtectedBrand;
